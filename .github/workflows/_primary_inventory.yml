name: Multisession primary inventory
on:
  push:
    branches:
      - scratch/multisession-primary-inventory
jobs:
  inventory:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: scratch/multisession-primary-inventory
      - name: Inventory direct primary coupling
        run: |
          set -euxo pipefail
          echo "=== direct session runtime ==="
          grep -RIn 'session_runtime' crates/forge-tui/src crates/forge-cli/src || true
          echo "=== primary/sibling guards ==="
          grep -RInE 'require_primary_task|selected_is_sibling|SelectedRuntime::Primary|SelectedRuntime::Sibling' crates/forge-tui/src || true
          echo "=== supervisor commands already routed ==="
          grep -RIn 'SupervisorCommand::' crates/forge-tui/src || true
